---
layout: post
title: 量化金融 投资组合 相关性分析
date: 2026-04-29 09:01:00 +0800
image: 21.jpg
tags:
  - financial
  - python
---

相关性分析的核心是计算 **皮尔逊相关系数（Pearson Correlation）**，其取值范围在 $-1$ 到 $1$ 之间：

- **1**：完全正相关（同涨同跌）。
    
- **0**：完全不相关（随机走势）。
    
- **-1**：完全负相关（对冲效果最佳）。

# 代码

```python
import yfinance as yf
import seaborn as sns
import matplotlib.pyplot as plt

tickers = ["COHR", "GLDM", "GOOGL", "NVDA", "QQQ", "VOO", "USD"]

data = yf.download(tickers, period="1y")['Adj Close']

# 3. 计算日收益率的相关性矩阵
corr_matrix = data.pct_change().corr()

# 4. 绘制热力图 (Heatmap)
plt.figure(figsize=(10, 8))
sns.heatmap(corr_matrix, annot=True, cmap='RdYlGn', center=0)
plt.title("Portfolio Correlation Matrix")
plt.show()
```